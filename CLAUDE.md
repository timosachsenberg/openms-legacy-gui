# CLAUDE.md

See [`AGENTS.md`](AGENTS.md) for how to work in this repository — build and test
commands, the OpenMS shared-data resolution order, portable-build behaviour,
macOS packaging hazards, and the shell conventions used in `ci/portable/`.

Those instructions apply to Claude Code as written; keep guidance in `AGENTS.md`
rather than duplicating it here, so the two cannot drift apart.
